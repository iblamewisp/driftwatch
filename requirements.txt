# Web
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# Database
sqlalchemy>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
pgvector>=0.2

# Task queue
celery>=5.3
redis>=5.0

# ML (numpy only — embeddings run in the separate litserve container)
# <2.0 pin: NumPy 2.0 is an ABI break; remove once torch/sentence-transformers confirm 2.x support
numpy>=1.26,<2.0

# Config & validation
pydantic>=2.6
pydantic-settings>=2.2

# Observability
structlog>=24.1
prometheus-client>=0.20
